import React, { PropTypes } from 'react'

import { RouteHandler} from 'react-router'

var LoggedOutHandler = React.createClass({
    render() {
        return (
            <RouteHandler/>
        )
    }
})

export default LoggedOutHandler
